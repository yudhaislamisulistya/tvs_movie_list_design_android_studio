<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.asistensi1dan2tahap1">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.tv_shows.TVShowDetailActivity"
            android:theme="@style/AppThemeV23">
        </activity>
        <activity
            android:name=".ui.movies.AllMovieActivity"
            android:theme="@style/AppThemeV23"></activity>
        <activity
            android:name=".ui.tv_shows.TVShowsActivity"
            android:theme="@style/AppThemeV23"></activity>
        <activity
            android:name=".ui.register.Register"
            android:theme="@style/RegisterTheme" />
        <activity
            android:name=".ui.login.Login"
            android:theme="@style/LoginTheme" />
        <activity
            android:name=".ui.movies.MovieDetailActivity"
            android:theme="@style/AppThemeV23" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppThemeV23" />
        <activity
            android:name=".ui.splash_screen.SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>